interface DeleteUserProps {
	open: boolean;
	onOpenChange: (open: boolean) => void;
	resetSelection: () => void;
}
export const DeleteUser = ({
	open,
	onOpenChange,
	resetSelection,
}: DeleteUserProps & {}) => {
	return <></>;
};
